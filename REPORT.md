# 严格增量 CCG 的无监督范畴归纳：实现与实验报告

代码：本仓库（`ccg/` 库、`scripts/` 实验脚本、`configs/default.yaml` 单一配置、`tests/` 单元测试）。
所有结果在 `results/` 下，可由 `scripts/make_tables.py` 汇总为 `results/tables.md`（本报告末尾附完整表格）。

复现：
```
pip install numpy pyyaml pytest
python3 scripts/prepare_data.py            # data/raw/*.conllu -> data/processed/*.jsonl
python3 -m pytest tests -q                  # 48 个单元测试（每条规则正反例、依存映射、模型/EM）
python3 scripts/run_upper_bound.py          # §5 手写上界
python3 scripts/run_synthetic.py --seeds 1,2,3,4,5,6,7,8 --init_support 4 --outer 15 --set mdl.curriculum=null --set mdl.patience=100 --set mdl.pair_proposals=false   # §7.3 人造数据
./scripts/run_queue.sh                      # 全部归纳配置（scripts/jobs.txt）
python3 scripts/run_phenomena.py --models 'results/induction/*/seed*_model.pkl'
python3 scripts/make_tables.py
```
所有随机种子写入结果 JSON（`seed` 字段）；每个配置的日志在 `results/induction/<config>/log_seeds*.txt`。

---

## 1. 形式系统（`ccg/category.py`, `ccg/combine.py`）

严格左分支（左梳）派生：状态 σ_k 为前缀范畴，σ_0 = ε，接受当且仅当 σ_n = S。规则完全按任务书：
FA、BA、B>、B<、SA（A 填入词范畴 c 脊上**非最外层**的后向槽；最外层后向槽由 BA 覆盖；若多个内层槽匹配取最外层并计数）、句首 LEX。无 type-raising、无前向 SA、无 CAL、无论元置换、无一元规则、无标点规则。状态深度上限 MAX_DEPTH（slash 嵌套深度，`depth(atom)=0`）默认 4。

`combine(σ, c)` 的结果按结果范畴去重；一个 (σ, c) 可有多个结果（如 B< 与 SA 同时适用）。派生概率中每步乘 1/n_app(σ,c)（可适用规则数的均匀选择），保证派生分布归一（`ccg/lattice.py`）。

单元测试 `tests/test_combine.py`：每条规则的正例与反例、SA 内层/外层槽区分、SA 槽不唯一取最外层、深度上限、被禁止规则不可用、整句类型检查、Viterbi 规则序列。

## 2. 数据（`ccg/data.py`, `scripts/prepare_data.py`）

- 主语料 UD English-GUM（下载于 2026-09-21 的 master 分支；EWT 同时预处理为备选，本报告未用于训练）。
- 训练只用 train 分割的词形；dev 用于全部调参与分析；**test 只在 §12 用一次**。
- 预处理：去掉全部 PUNCT（head 指向标点的词重挂到标点的 head）、小写化（配置项 `lowercase`）、长度 ≤ 10（另报 ≤ 8）。
- 句子选择：GUM 的 `s_type=decl`（陈述句；用于筛除标题/片段/问句，这是唯一用到的元数据，训练时不使用任何标注）。
- 规模：train 2012 句（≤8: 1481）、dev 289 句（≤8: 214）、test 212 句。train 词型 3181，dev 词元 1941 其中 19% 在 train 中未见（OOV）。

## 3. 模型与算法

### 3.1 范畴空间（`category.enumerate_categories`）
原子集合按消融组。枚举 arity ≤ 3、深度 ≤ 3 的全部范畴；**额外界**（见"默认值"）：总 slash 数 ≤ 4、至多 1 个复杂论元、复杂论元的 arity ≤ 2 且其论元为原子。三原子时全空间 14493；再按 §1 的"标准槽位形状"过滤（所有后向槽在前向槽内侧，即 (X\Y)/Z 形；禁 TR 形 X/(X\Y)）得 7914。先验 P(c) ∝ 2^-|c|，|c| = 原子数+slash 数。

### 3.2 推断（`ccg/lattice.py`）
对每句先构造前缀状态格（前向展开所有 (σ, c) 组合，再反向剪除不能到达 S 的状态），格上做前向后向 / Viterbi。格在词库支撑集固定时缓存，EM 内层只做数值遍历。实测分支因子 b（每个 (σ, w_k) 的后继状态数均值）与 |Q|（每位置未剪枝状态数均值）逐轮记录（见 §7 表）。未使用束剪枝（实测 |Q| 很小，见结果表）；实现中保留了按目标原子配额的多样性束（`beam` 参数，默认关闭）。

### 3.3 学习（`ccg/model.py`, `ccg/em.py`, `ccg/mdl.py`, `ccg/induce.py`）

**⚠ 偏离 1（目标函数）**：任务书写"EM 估计 P(c|w)"、L(D|M) = −log₂P(语料)。字面实现（`learning.model: conditional`）的数据似然是 P(接受 | 词序列) = Σ_派生 Π P(c_k|w_k)/n_app。人造数据实验证明该目标**无法辨识目标词库**：把 N、NP、S 合并成一个原子的"塌缩"词库能刚性地分析全部句子（P=1），且词条更少，因此严格优于金标词库（146 bit vs 156 bit，§6）。条件式目标从不为过度生成付费。因此主实验采用**生成式**模型：

P(词序列, 派生) = Π_k P(c_k | σ_{k−1}) · P(w_k | c_k) · 1/n_app(σ_{k−1}, c_k) · P(stop | σ_n = S)

转移 P(c|σ) 用 Witten-Bell 插值到全局范畴分布（β=1），发射 P(w|c) 限制在词库支撑集内（γ=0.01）。EM 在同一前缀状态格上做前向后向；报告的 P(c|w) 为期望计数归一化。L(D|M) = −log₂P(词序列)。两种目标都实现，配置切换；§6 给出对照。

**L(M)** = Σ_词条 (|c| + log₂|V|)，|V| 为参数键数（词–范畴配对代价取 log₂|V|）。转移/发射参数不计入 L(M)（默认值，见附录）。

**无法解析句子的代价**：MDL 需要有限；每个似然为 0 的句子按逐词显式编码计 n·(log₂|范畴池| + log₂|V| + 1) bit（`escape_bits_per_word`）。这不是兜底范畴：学习器不会给任何词添加范畴，除非 ΔMDL < 0。

**初始化**：分布聚类（左右邻词 PPMI 向量 k-means，K=24，`ccg/clustering.py`）+ 噪声：P₀(c|w) ∝ 2^-|c|·exp(ε_cluster(c) + 0.5·ε_w(c))，ε~N(0,1)，同簇词共享 ε_cluster。每词从 P₀ 中不放回抽 `init_support`=4 个范畴（≤2 slash 的池）。稀疏初始化是有意的：实验表明稠密随机初始化（每词 12 个范畴）使 EM 陷入多种分析混杂的局部最优（§6）。频次 < 2 的词共享其簇的参数（键 `<Ck>`；这是参数绑定，不是兜底范畴；dev 中未见词无词条、直接计失败）。

**外层 MDL 循环**（EM 收敛后执行，直到词库不再变化或达到轮数上限）：
- 剪枝：候选为期望计数 < 0.05 或 < 0.5·count(w) 的词条；逐个评估，删除当且仅当 ΔMDL < 0。评估时被删词条的发射质量按比例转给该词其余范畴（近似重估）。
- 分裂：候选为 H(c|w) ≥ 0.5 bit 且其出现按前缀状态可分为主/次两组（次组 ≥ 20%）的词；对次组前缀状态用"单词神谕"生成候选范畴 c'（给该词在该位置换成 c' 后整句在其它词支撑集不变下可解析），按 (修复的出现次数)·2^-|c'| 排序取前 6，逐个评估，执行当且仅当 ΔMDL < 0（每词每轮最多 3 个）。
- 合并：范畴对上下文分布（前缀状态分布）JS 散度 ≤ 0.3 者为候选（最多 2000 对），c1→c2 全局替换，执行当且仅当 ΔMDL < 0。
- 所有阈值只缩小搜索。

**⚠ 偏离 2（额外的候选生成，均受同一 ΔMDL<0 准则约束）**：
(a) **失败驱动候选**：对解析失败的句子，在失败位置及其前 3 个词上用上述单词神谕生成候选（§3.3 分裂只针对已能解析、熵高的词，不足以让稀疏词库生长）。
(b) **双词候选**（`pair_step`）：对单词修改无法修复的句子，先给失败点前的词试加一个单词候选，再对新的失败点做神谕，成对加入。长句几乎都需要两处协同修改。
(c) **全局重命名**（作为合并的推广）：把在词条内部出现的复杂子范畴 X 全局替换为未使用的原子（双射重标，派生不变、L(M) 下降）。
(d) **句长课程**：轮次按句长阶段 ≤3、≤5、≤7、≤10 推进（每阶段最多 5 轮，或词库不变/目标 2 轮无改善即进入下一阶段）。真实数据上没有课程时词库在首轮剪枝后塌缩到每词 1 个范畴、随后无法生长（600 句切片：无课程 100/600 可解析 → 有课程+双词候选 589/600）。**但**人造数据（20 词、平均 4.7 词）上课程反而有害（§6），因此 §6 报告两种调度。

### 3.4 失败日志
每个似然为 0 的句子记录：句子、失败位置 k、σ_{k−1} 候选集、w_k、w_k 的候选范畴（`lattice.failure_record`）；dev 上另附该词的 UPOS/deprel 用于按构式聚类（`experiment.failure_clusters`）。

## 4. 评测（`ccg/deps.py`, `ccg/evaluate.py`, `ccg/experiment.py`）

**依存读出**：每次 FA/BA/B>/B</SA 记录一个事件 (函子词 f, f 词范畴上的槽位 i, 论元中心 a)。槽位归属经过组合传递（B> 得到的 X/Z 中 Z 槽属于提供 Y/Z 的词），因此 UD 式中心词由词范畴上的槽位静态决定。

**中心词映射模块**（`deps.HeadMap`，可配置，四个开关 `flip_function_words / flip_modifiers / flip_cc / raised_subjects`）：
- 功能词（det/aux/cop/case/mark 词表）的**标记槽**：det/case 词取最外层前向 N/NP 论元槽（跳过"提升主语"槽；范畴呈动词形 (S\NP)... 时不标记）；aux/mark 取最外层 S 目标的前向槽；cop 同 aux、否则最外层前向槽。功能词挂到标记槽论元的短语中心，其它论元也挂到该中心（UD 内容词中心约定）。
- 修饰语槽：论元范畴 = 该层结果范畴（X|X）；或论元与函子都以 S 为目标且函子没有主语槽（如 (S/(S\NP))/NP 的前主语副词/叹词）。论元中心成为短语中心，函子短语挂到它。动词形范畴的内层 X|X 槽（如 ((S\NP)/(S\NP))/NP 的 VP 补足语）与控制动词 (S\NP)/(S\NP) 按补足语处理（控制动词词表）。
- 提升主语槽：在 S\NP 型小句论元外侧的第一个 NP 槽（(S/(S\NP))/NP、((S\NP)/(S\NP))/NP、((S\S)/(S\NP))/NP）中的 NP 挂到该小句中心（宾语控制动词词表除外：挂到函子）。
- cc：(X\X)/X 形的并列词：左并列项为短语中心，右并列项挂到左（conj），并列词挂到右并列项（cc）。
- 所有格 's：(NP/N)\NP 形，'s 挂到所有者、所有者挂到被所有名词。
- 根 = 终态中心词的短语中心。
这些规则以词表和范畴形状为条件，对手写和归纳词库同样适用（原子被重命名时也不受影响，因为只看结构）。`tests/test_deps.py` 覆盖了及物句、限定词、助动词链、系词+程度副词、PP 论元/附加语、句首叹词/PP 带主语、主语/宾语关系化、NP 并列、零补语 ccomp、宾语控制、句末从句、所有格、名词 PP 补足语、词汇化 TR。

**指标**：UAS（去标点后全部词，根记 head=0；未覆盖句子记全错，另报覆盖句子子集与"词汇内句子"子集）；覆盖率；最小对立句成对准确率；逐词困惑度（覆盖句子上 exp(−log P/词数)，生成式与条件式不可比）；结构监控（范畴总数、每词平均范畴数、b、|Q| 逐轮）。基线（`ccg/baselines.py`）：左分支（每词挂到左邻、首词为根）、右分支（挂到右邻、末词为根）、随机树（5 个种子）。括号 F1（左梳的所有前缀 vs 金依存树的投射 yield）只作附录。

**现象测试集**（`ccg/phenomena.py`）：三组，正例按 UD 模式从 dev（不足 20 时补 train，来源标注）抽取，负例按构式相关变换构造（限定词后置、及物动词与宾语互换/主语后置、助动词后置、"通用"重复功能词等）；RNR、非成分并列、寄生空位、gapping、比较句在 ≤10 词语料中几乎不存在，用构造例（标注 constructed）。判据 L1 接受 / L2 拒绝负例 / L3 构式核心弧（各现象定义的 UD 弧集合）全部正确 / L4 未用构式专属范畴（手写：显式清单；归纳：构式功能词在此派生中的范畴等于它在 dev 上的多数范畴）。另报"词汇内"子集。

## 5. 手写词库上界（`ccg/handwritten.py`, `scripts/run_upper_bound.py`）

手写 755 个词的词条，按 train+dev 频次截取前 500 词（任务书 300–500）；原子 {S,N,NP,PP}（B 组），A 组版本由 PP→NP 重命名得到；TR 变体给主语代词/限定词/名词加 S/(S\NP)、(S/(S\NP))/N。词类由 UD UPOS 辅助确定，范畴形状全部手写。**严格左分支迫使的写法**（这是上界实验最主要的发现，也是后面归纳结果的解释）：
1. 主语之前的一切（叹词、句首副词、句首连词、句首 PP）必须**吃掉主语**：yeah/so/and : (S/(S\NP))/NP，in : ((S/(S\NP))/NP)/NP。否则 S/S + NP 无规则。
2. 动词短语一旦归约为 S，VP 修饰语只能写成 S\S；时间 NP 状语要靠限定词 (S\S)/N。
3. 嵌套小句的主语必须由前一个词吃掉：think : ((S\NP)/(S\NP))/NP；补语标记 that + 主语在 SA 版本中**不可派生**（需要 arity 4 或词汇化 TR），只在 TR 变体中可派生。
4. 宾语关系化需要 ((NP\NP)/((S\NP)/NP))/NP 这种构式专属范畴。
5. 主语之前有修饰语时动词的 arity 必须 ≤ 2：(S/(S\NP))/NP + NP → S/(S\NP)，再与 ((S\NP)/PP)/NP 无法 B>（无广义组合）。
6. 名词的 of-补足语只能在名词未被吸收前附着：写成 N/PP。
7. VP 并列 "i sing and dance" 只能写成 and : (S\S)/(S\NP)（第二个动词失去主语；L3 必错）。

结果（dev ≤10 / ≤8，见附表"手写上界"）：在手写词汇内的句子上（n=90），SA 版覆盖率 65.6%，TR 版 71.1%；覆盖句子上 UAS 92.8%（右分支基线在同一子集 40.7%）；全部 dev 句子上覆盖率 20.4%（受 500 词上限与 19% OOV 限制）；SA 槽不唯一事件 0/54。止损条件（上界覆盖率 < 60%）按"词汇内覆盖率"判断为通过；按"全 dev 覆盖率"则不通过——这一差别完全来自词汇量，不来自形式系统，故继续进入学习阶段，但在此明确指出。

失败分布（SA_B，dev≤10 词汇内 31 个失败）：PRON/nsubj 23（主语前有材料且材料未写成吃主语的形式，或嵌套主语）、句末未成 S 8、AUX/cop 4、DET/det 3……详见 `results/upper_bound/SA_B_le10.json`。

## 6. 人造数据验证（`ccg/synthetic.py`, `scripts/run_synthetic.py`）

20 词语法（4 专名 NP、2 限定词 NP/N、3 名词 N、2 不及物 S\NP、2 及物 (S\NP)/NP、2 形容词 N/N、2 副词 S\S、2 介词 (S\S)/NP 与 (NP\NP)/NP、1 并列词 ((S\S)/(S\NP))/NP），随机生成 500 句（平均 4.7 词，全部可由金标词库派生）。

先验证目标函数（金标词库参数由 EM 拟合）：

| 词库 | 生成式 L(M)+L(D|M) | 条件式 L(M)+L(D|M) |
|---|---|---|
| 金标 | 156 + 5932 = **6088** | 156 + 0 = 156 |
| 塌缩（N,NP→S） | 146 + 8654 = 8801 | 146 + 0 = **146** |

条件式目标下塌缩词库更优（不可辨识）；生成式目标下金标最优。

学习结果（8 个种子，init_support=4，`derivation recovery` = 学得 Viterbi 派生与金标派生的原始函子→论元依存一致率；`majority acc` = 按 N↔NP 最优重命名后每词多数范畴与金标相等的比例——它把派生等价的变体（如 (S/NP)\S 代替 (S\S)/NP、更便宜的并列词范畴）也算错，所以偏低）：

| 调度 | derivation recovery 均值±sd | MDL 选出的种子 | 其 bits |
|---|---|---|---|
| S1：无课程、无双词候选（`--set mdl.curriculum=null --set mdl.patience=100 --set mdl.pair_proposals=false`） | 0.683 ± 0.180 | 0.972（20 词条、9 范畴） | 6091（金标 6088） |
| S2：真实数据用的调度（课程+双词候选+早停） | 0.548 ± 0.258 | 0.641 | 6219 |
| 条件式目标（S1） | 0.398 ± 0.108 | 0.241（塌缩词库） | 156 |

结论：目标函数正确（金标是最优点，MDL 选出的 S1 结果达到金标目标值并恢复 97% 的派生依存；剩余差异是并列词的更便宜分析 (N\S)/N 和 N↔NP 重命名），但**搜索是多峰的**：单个种子平均只恢复 68%，必须用 MDL 在多个重启中选择（无标注信息）。这一"通过"是有条件的：需要 8 次重启并按目标值选择；把真实数据所需的课程调度用于人造数据会变差。稠密初始化（每词 12 个范畴）、硬 EM、退火 EM 均不能恢复（分别 0.35/0.25/0.65 多数准确率，未列表）。

## 7. 真实数据（GUM）归纳结果

（见附表"归纳结果"；本节文字在实验完成后填写）

RESULTS_PLACEHOLDER

## 附录 A：未指定细节的默认值（集中列出）
- 范畴空间额外界：slash ≤ 4（C 组 ≤ 3）、复杂论元 ≤ 1、复杂论元 arity ≤ 2 且论元原子；标准槽位过滤；主系统禁 TR 形。
- depth 定义：depth(atom)=0，depth(X|Y)=1+max。MAX_DEPTH 作用于前缀状态。
- 规则重叠：同一 (σ,c) 的多个结果各以 1/n_app 加权。
- 生成式模型：Witten-Bell β=1；发射 γ=0.01；全局范畴分布加 2^-|c| 先验（κ=1）；P(stop|S) 用 (n_stop+0.5)/(n+1)。
- L(M) 不含参数编码；escape 代价见 §3.3。
- 聚类：K=24（D 组用 3 个粗簇定义原子 C0..C2），PPMI 左右邻词各 200 维，k-means++ 30 轮。
- 初始化：init_support=4，init 池 ≤2 slash，噪声尺度 1.0。
- 稀有词阈值 min_freq=2。
- EM：≤20 轮，相对容差 1e-3；外层 ≤24 轮；课程 [3,5,7,10] 每阶段 ≤5 轮，patience 2。
- 候选：剪枝相对阈值 0.5·count；分裂熵阈值 0.5 bit、次组 ≥20%；合并 JS ≤ 0.3；每词每轮 ≤3 新范畴、每词候选 6；失败回看 3 词；双词候选每轮 ≤300 句。
- 新词条初始质量 0.2·count(w)。
- 中心词映射词表：`ccg/deps.py` 顶部 DET/AUX/COP/CASE/MARK/CC/OBJ_CONTROL/CONTROL。
- 负例变换：`phenomena.make_negative`。
- 现象正例：dev 优先，不足 20 补 train；构造例见 `phenomena.CONSTRUCTED`。

## 附录 B：未核实事项
- Evang 2020 的可配置依存抽取只按记忆参考其"按范畴槽位静态标记中心"的思路，具体论文标题/细节未核实。
- UD GUM 版本：从 GitHub master 直接下载（2026-09-21），未固定到某个正式发布版本。
- "Baby steps" 式句长课程（Spitkovsky 等）作为已知技巧引用，未核实出处细节。
